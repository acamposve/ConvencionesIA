# Embeddings Feature Domain

## Core Concepts

- Embedding availability: whether the system has generated embeddings for the document.
- Embedding readiness: whether the embeddings are ready for downstream use.

## Key Behaviors

- The UI should expose embedding availability in a clear, approachable way.
- Missing embeddings should not break the document experience.
- The feature should remain informational rather than operational.
